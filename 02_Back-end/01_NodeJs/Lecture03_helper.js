// To use the module (obj) we need to require the obj

const obj = require("./Lecture03");

// Access the average function
console.log(obj.average(10, 20));

// Access the multiply function
console.log(obj.multiply(2, 4));